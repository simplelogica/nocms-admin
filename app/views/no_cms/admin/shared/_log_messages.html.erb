<% types ||= [:info, :warning, :error] %>
<% @logger.messages(types).each do |type, messages| %>
  <% messages.each do |message| %>
    <div class="row msg <%= type %>">
      <%= message %>
    </div>
  <% end %>
<% end %>
